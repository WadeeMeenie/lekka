# Supabase connection verification

The configured Supabase MCP discovered one active project: `local-radar-sa`, project reference `vnitwsjidlurlwlpsmtf`, region `eu-west-1`, status `ACTIVE_HEALTHY`, database host `db.vnitwsjidlurlwlpsmtf.supabase.co`, Postgres 17.6.1.155. The configured client URL and publishable key were verified against the project Auth settings endpoint without printing credentials; the response status was HTTP 200. No service-role credential was used or stored in the Android app.
