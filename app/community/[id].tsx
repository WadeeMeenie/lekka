import { useCallback, useEffect, useState } from "react";
import { ActivityIndicator, Alert, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { router, useLocalSearchParams } from "expo-router";

import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { useAuth } from "@/hooks/use-auth";
import { useColors } from "@/hooks/use-colors";
import { canManageCommunityMember, getCommunityMemberRoleLabel } from "@/lib/community-management";
import { CommunityMember, getCommunity, getCommunityMembershipState, joinCommunity, leaveCommunity, listCommunityMembers, removeCommunityMember, subscribeToCommunityMembers, updateCommunityMemberRole } from "@/lib/local-directory";

type Community = { id: string; name: string; description: string | null; area: string; category: string; rules: string[]; created_by: string; created_at: string };

export default function CommunityDetailScreen() {
  const colors = useColors();
  const { id } = useLocalSearchParams<{ id: string }>();
  const { isAuthenticated, user } = useAuth();
  const [community, setCommunity] = useState<Community | null>(null);
  const [members, setMembers] = useState<CommunityMember[]>([]);
  const [memberCount, setMemberCount] = useState(0);
  const [isMember, setIsMember] = useState(false);
  const [membershipLoading, setMembershipLoading] = useState(false);
  const [membersLoading, setMembersLoading] = useState(true);
  const [membershipError, setMembershipError] = useState<string | null>(null);
  const [loading, setLoading] = useState(true);
  const isOwner = Boolean(user?.id && community?.created_by === String(user.id));

  const refreshMembers = useCallback(async () => {
    if (!id) return;
    const [membershipResult, membersResult] = await Promise.all([getCommunityMembershipState(id), listCommunityMembers(id)]);
    setMemberCount(membershipResult.data.memberCount);
    setIsMember(membershipResult.data.isMember);
    setMembers(membersResult.data);
    setMembershipError(membershipResult.error?.message || membersResult.error?.message || null);
    setMembersLoading(false);
  }, [id]);

  useEffect(() => { let active = true; if (!id) return () => { active = false; }; void Promise.all([getCommunity(id), refreshMembers()]).then(([communityResult]) => { if (active) setCommunity(communityResult.data as Community | null); }).finally(() => { if (active) setLoading(false); }); const unsubscribe = subscribeToCommunityMembers(id, () => { void refreshMembers(); }); return () => { active = false; unsubscribe(); }; }, [id, refreshMembers]);

  const handleMembership = async () => { if (!id) return; if (!isAuthenticated) { Alert.alert("Sign in to join", "Create or sign in to a Lekka account to join this community."); return; } setMembershipLoading(true); setMembershipError(null); const result = isMember ? await leaveCommunity(id) : await joinCommunity(id); if (result.error) setMembershipError(result.error.message); else { await refreshMembers(); } setMembershipLoading(false); };
  const handleRole = async (member: CommunityMember) => { if (!id) return; const next = !member.is_moderator; setMembershipLoading(true); setMembershipError(null); const result = await updateCommunityMemberRole(id, member.user_id, next); if (result.error) setMembershipError(result.error.message); else await refreshMembers(); setMembershipLoading(false); };
  const handleRemove = (member: CommunityMember) => { if (!id) return; Alert.alert("Remove member?", `Remove ${member.profiles?.display_name || "this member"} from the community?`, [{ text: "Cancel", style: "cancel" }, { text: "Remove", style: "destructive", onPress: () => { void (async () => { setMembershipLoading(true); const result = await removeCommunityMember(id, member.user_id); if (result.error) setMembershipError(result.error.message); else await refreshMembers(); setMembershipLoading(false); })(); } }]); };

  if (loading) return <ScreenContainer><View style={styles.center}><Text style={[styles.status, { color: colors.muted }]}>Loading community…</Text></View></ScreenContainer>;
  if (!community) return <ScreenContainer><View style={styles.center}><Text style={[styles.status, { color: colors.foreground }]}>This community is no longer available.</Text><Pressable onPress={() => router.back()}><Text style={[styles.link, { color: colors.primary }]}>Go back</Text></Pressable></View></ScreenContainer>;
  return <ScreenContainer edges={["top", "bottom", "left", "right"]}><ScrollView contentContainerStyle={styles.content}><Pressable onPress={() => router.back()} style={styles.back}><IconSymbol name="chevron.right" size={20} color={colors.foreground} style={styles.backIcon} /><Text style={[styles.backText, { color: colors.foreground }]}>Social</Text></Pressable><View style={[styles.hero, { backgroundColor: colors.primary }]}><Text style={styles.initial}>{community.name.slice(0, 1).toUpperCase()}</Text></View><Text style={[styles.eyebrow, { color: colors.primary }]}>PUBLIC COMMUNITY</Text><Text style={[styles.title, { color: colors.foreground }]}>{community.name}</Text><Text style={[styles.meta, { color: colors.muted }]}>{community.area} · {community.category} · {memberCount} {memberCount === 1 ? "member" : "members"}</Text><Pressable onPress={() => void handleMembership()} disabled={membershipLoading} accessibilityRole="button" accessibilityLabel={isMember ? "Leave community" : "Join community"} style={[styles.joinButton, { backgroundColor: isMember ? colors.surface : colors.primary, borderColor: colors.primary, opacity: membershipLoading ? 0.7 : 1 }]}>{membershipLoading ? <ActivityIndicator color={isMember ? colors.primary : colors.background} /> : <Text style={[styles.joinText, { color: isMember ? colors.primary : colors.background }]}>{isMember ? "Leave community" : "Join community"}</Text>}</Pressable>{membershipError && <Text accessibilityRole="alert" style={[styles.error, { color: colors.error }]}>{membershipError}</Text>}<View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[styles.cardTitle, { color: colors.foreground }]}>About this community</Text><Text style={[styles.body, { color: colors.muted }]}>{community.description || "This community has not added a description yet."}</Text></View>{community.rules?.length > 0 && <View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[styles.cardTitle, { color: colors.foreground }]}>Community guidelines</Text><Text style={[styles.body, { color: colors.muted }]}>{community.rules.join("\n")}</Text></View>}<View style={styles.sectionHeader}><Text style={[styles.sectionTitle, { color: colors.foreground }]}>Members</Text><Text style={[styles.sectionMeta, { color: colors.muted }]}>{membersLoading ? "Loading…" : `${members.length} shown`}</Text></View>{membersLoading ? <ActivityIndicator color={colors.primary} /> : members.length === 0 ? <Text style={[styles.body, { color: colors.muted }]}>No members have joined yet.</Text> : members.map((member) => <View key={member.user_id} style={[styles.member, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={[styles.memberAvatar, { backgroundColor: colors.primary }]}><Text style={styles.memberInitial}>{(member.profiles?.display_name || "?").slice(0, 1).toUpperCase()}</Text></View><View style={styles.memberCopy}><Text style={[styles.memberName, { color: colors.foreground }]}>{member.profiles?.display_name || "Lekka member"}</Text><Text style={[styles.memberMeta, { color: colors.muted }]}>{member.profiles?.username ? `@${member.profiles.username}` : "Member"}{` · ${getCommunityMemberRoleLabel(member.is_moderator)}`}</Text></View>{canManageCommunityMember(community.created_by, user?.id, member.user_id) && <View style={styles.memberActions}><Pressable onPress={() => void handleRole(member)} accessibilityLabel={member.is_moderator ? "Remove moderator role" : "Make moderator"}><IconSymbol name="shield.fill" size={18} color={member.is_moderator ? colors.primary : colors.muted} /></Pressable><Pressable onPress={() => handleRemove(member)} accessibilityLabel="Remove member"><IconSymbol name="trash.fill" size={18} color={colors.error} /></Pressable></View>}</View>)}{isOwner && <View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[styles.cardTitle, { color: colors.foreground }]}>Owner controls</Text><Text style={[styles.body, { color: colors.muted }]}>Use the shield icon to grant or remove moderator access. Use the trash icon to remove a member.</Text></View>}<View style={[styles.empty, { borderColor: colors.border }]}><IconSymbol name="bubble.left.fill" size={26} color={colors.muted} /><Text style={[styles.emptyTitle, { color: colors.foreground }]}>Conversation coming next</Text><Text style={[styles.body, { color: colors.muted }]}>Community posts will appear here as members start sharing local updates.</Text></View></ScrollView></ScreenContainer>;
}

const styles = StyleSheet.create({ content: { padding: 20, paddingBottom: 42 }, center: { flex: 1, alignItems: "center", justifyContent: "center", gap: 10, padding: 24 }, status: { fontSize: 16, fontWeight: "800", textAlign: "center" }, link: { fontWeight: "800" }, back: { flexDirection: "row", alignItems: "center", gap: 5, marginBottom: 22 }, backIcon: { transform: [{ rotate: "180deg" }] }, backText: { fontSize: 14, fontWeight: "800" }, hero: { width: 76, height: 76, borderRadius: 25, alignItems: "center", justifyContent: "center", marginBottom: 18 }, initial: { color: "#FFF", fontSize: 32, fontWeight: "900" }, eyebrow: { fontSize: 10, fontWeight: "800", letterSpacing: 1.1 }, title: { fontSize: 28, lineHeight: 34, fontWeight: "900", marginTop: 6 }, meta: { fontSize: 13, marginTop: 5 }, joinButton: { borderWidth: 1, borderRadius: 14, minHeight: 46, alignItems: "center", justifyContent: "center", marginTop: 18 }, joinText: { fontSize: 15, fontWeight: "900" }, error: { fontSize: 13, lineHeight: 18, marginTop: 8 }, card: { borderWidth: 1, borderRadius: 18, padding: 16, marginTop: 20 }, cardTitle: { fontSize: 16, fontWeight: "900" }, body: { fontSize: 14, lineHeight: 21, marginTop: 7 }, sectionHeader: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginTop: 26, marginBottom: 10 }, sectionTitle: { fontSize: 20, fontWeight: "900" }, sectionMeta: { fontSize: 12 }, member: { borderWidth: 1, borderRadius: 15, padding: 11, flexDirection: "row", alignItems: "center", marginBottom: 8 }, memberAvatar: { width: 42, height: 42, borderRadius: 15, alignItems: "center", justifyContent: "center" }, memberInitial: { color: "#FFF", fontSize: 18, fontWeight: "900" }, memberCopy: { flex: 1, marginLeft: 11 }, memberName: { fontSize: 14, fontWeight: "800" }, memberMeta: { fontSize: 12, marginTop: 3 }, memberActions: { flexDirection: "row", alignItems: "center", gap: 16 }, empty: { borderWidth: 1, borderStyle: "dashed", borderRadius: 18, padding: 21, alignItems: "center", gap: 7, marginTop: 20 }, emptyTitle: { fontSize: 15, fontWeight: "800" },
});
